
name: Remove Demo Resource Groups

on:
  workflow_dispatch:

jobs:

  rm-resource-groups:
    runs-on: ubuntu-latest
    steps:
    
    - name: Azure login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

